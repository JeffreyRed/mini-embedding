name: mini-embedding
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch>=2.0.0
  - torchvision
  - cpuonly                  # remove this line if you have a GPU
  - matplotlib>=3.7.0
  - numpy>=1.24.0
  - pip
